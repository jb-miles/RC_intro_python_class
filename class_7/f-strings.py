from datetime import datetime as dt
from math import pi as pi

name = "Rob"
current_date = dt.now().date()
print(f"My name is {name}. The current date is {current_date}")
print(f"π to the second digit is {pi:.2f}")
